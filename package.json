{
  "name": "blockai",
  "version": "1.1.0",
  "description": "Chrome extension: VPN gate before Claude — no IP leak to Anthropic",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/sync-manifest-version.mjs && vite build",
    "pack": "bash scripts/pack-extension.sh",
    "icons": "python3 scripts/generate-icons.py",
    "postinstall": "npm run build"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.4.5",
    "vite": "^5.4.2"
  }
}
